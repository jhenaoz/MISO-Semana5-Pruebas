class Staff {
    constructor(page) {
        this.page = page;
      }

    async go(url, password) {
        await this.page.fill('#ember8', username);
        return true;
    }
}

module.exports = { Staff };
